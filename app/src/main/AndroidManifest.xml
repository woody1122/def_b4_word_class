<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.oscebosskey">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".HistoryActivity"
            android:label="History"></activity>
        <activity
            android:name=".ExaminationActivity"
            android:label="Examination"></activity>
        <activity
            android:name=".OtherActivity"
            android:label="Other"></activity>

        <activity
            android:name=".ChestPain"
            android:label="Chest Pain"></activity>
        <activity
            android:name=".AbdominalPain"
            android:label="Abominal Pain"></activity>
        <activity
            android:name=".Headache"
            android:label="Headache"></activity>

    </application>

</manifest>
